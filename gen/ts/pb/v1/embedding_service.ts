// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: pb/v1/embedding_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "pb.v1";

export interface EmbedTextRequest {
  text: string;
}

export interface EmbedTextResponse {
  embedding: number[];
}

export interface SearchFilesRequest {
  query: string;
  k: number;
}

export interface SearchResult {
  fileId: number;
  distance: number;
}

export interface SearchFilesResponse {
  results: SearchResult[];
}

export interface AddFileRequest {
  fileId: number;
  embedding: number[];
}

export interface AddFileResponse {
  success: boolean;
  message: string;
}

function createBaseEmbedTextRequest(): EmbedTextRequest {
  return { text: "" };
}

export const EmbedTextRequest: MessageFns<EmbedTextRequest> = {
  encode(message: EmbedTextRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmbedTextRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmbedTextRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmbedTextRequest {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: EmbedTextRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<EmbedTextRequest>): EmbedTextRequest {
    return EmbedTextRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmbedTextRequest>): EmbedTextRequest {
    const message = createBaseEmbedTextRequest();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseEmbedTextResponse(): EmbedTextResponse {
  return { embedding: [] };
}

export const EmbedTextResponse: MessageFns<EmbedTextResponse> = {
  encode(message: EmbedTextResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.embedding) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmbedTextResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmbedTextResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 13) {
            message.embedding.push(reader.float());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.embedding.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmbedTextResponse {
    return {
      embedding: globalThis.Array.isArray(object?.embedding)
        ? object.embedding.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: EmbedTextResponse): unknown {
    const obj: any = {};
    if (message.embedding?.length) {
      obj.embedding = message.embedding;
    }
    return obj;
  },

  create(base?: DeepPartial<EmbedTextResponse>): EmbedTextResponse {
    return EmbedTextResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EmbedTextResponse>): EmbedTextResponse {
    const message = createBaseEmbedTextResponse();
    message.embedding = object.embedding?.map((e) => e) || [];
    return message;
  },
};

function createBaseSearchFilesRequest(): SearchFilesRequest {
  return { query: "", k: 0 };
}

export const SearchFilesRequest: MessageFns<SearchFilesRequest> = {
  encode(message: SearchFilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.k !== 0) {
      writer.uint32(16).int32(message.k);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchFilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchFilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.k = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchFilesRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      k: isSet(object.k) ? globalThis.Number(object.k) : 0,
    };
  },

  toJSON(message: SearchFilesRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.k !== 0) {
      obj.k = Math.round(message.k);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchFilesRequest>): SearchFilesRequest {
    return SearchFilesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchFilesRequest>): SearchFilesRequest {
    const message = createBaseSearchFilesRequest();
    message.query = object.query ?? "";
    message.k = object.k ?? 0;
    return message;
  },
};

function createBaseSearchResult(): SearchResult {
  return { fileId: 0, distance: 0 };
}

export const SearchResult: MessageFns<SearchResult> = {
  encode(message: SearchResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileId !== 0) {
      writer.uint32(8).int32(message.fileId);
    }
    if (message.distance !== 0) {
      writer.uint32(21).float(message.distance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fileId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.distance = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResult {
    return {
      fileId: isSet(object.fileId) ? globalThis.Number(object.fileId) : 0,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
    };
  },

  toJSON(message: SearchResult): unknown {
    const obj: any = {};
    if (message.fileId !== 0) {
      obj.fileId = Math.round(message.fileId);
    }
    if (message.distance !== 0) {
      obj.distance = message.distance;
    }
    return obj;
  },

  create(base?: DeepPartial<SearchResult>): SearchResult {
    return SearchResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchResult>): SearchResult {
    const message = createBaseSearchResult();
    message.fileId = object.fileId ?? 0;
    message.distance = object.distance ?? 0;
    return message;
  },
};

function createBaseSearchFilesResponse(): SearchFilesResponse {
  return { results: [] };
}

export const SearchFilesResponse: MessageFns<SearchFilesResponse> = {
  encode(message: SearchFilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      SearchResult.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchFilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchFilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(SearchResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchFilesResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => SearchResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchFilesResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => SearchResult.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SearchFilesResponse>): SearchFilesResponse {
    return SearchFilesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchFilesResponse>): SearchFilesResponse {
    const message = createBaseSearchFilesResponse();
    message.results = object.results?.map((e) => SearchResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAddFileRequest(): AddFileRequest {
  return { fileId: 0, embedding: [] };
}

export const AddFileRequest: MessageFns<AddFileRequest> = {
  encode(message: AddFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileId !== 0) {
      writer.uint32(8).int64(message.fileId);
    }
    writer.uint32(18).fork();
    for (const v of message.embedding) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fileId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag === 21) {
            message.embedding.push(reader.float());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.embedding.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFileRequest {
    return {
      fileId: isSet(object.fileId) ? globalThis.Number(object.fileId) : 0,
      embedding: globalThis.Array.isArray(object?.embedding)
        ? object.embedding.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: AddFileRequest): unknown {
    const obj: any = {};
    if (message.fileId !== 0) {
      obj.fileId = Math.round(message.fileId);
    }
    if (message.embedding?.length) {
      obj.embedding = message.embedding;
    }
    return obj;
  },

  create(base?: DeepPartial<AddFileRequest>): AddFileRequest {
    return AddFileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddFileRequest>): AddFileRequest {
    const message = createBaseAddFileRequest();
    message.fileId = object.fileId ?? 0;
    message.embedding = object.embedding?.map((e) => e) || [];
    return message;
  },
};

function createBaseAddFileResponse(): AddFileResponse {
  return { success: false, message: "" };
}

export const AddFileResponse: MessageFns<AddFileResponse> = {
  encode(message: AddFileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddFileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFileResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: AddFileResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<AddFileResponse>): AddFileResponse {
    return AddFileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddFileResponse>): AddFileResponse {
    const message = createBaseAddFileResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

export type EmbeddingServiceService = typeof EmbeddingServiceService;
export const EmbeddingServiceService = {
  embedText: {
    path: "/pb.v1.EmbeddingService/EmbedText",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: EmbedTextRequest) => Buffer.from(EmbedTextRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => EmbedTextRequest.decode(value),
    responseSerialize: (value: EmbedTextResponse) => Buffer.from(EmbedTextResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => EmbedTextResponse.decode(value),
  },
  searchFiles: {
    path: "/pb.v1.EmbeddingService/SearchFiles",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SearchFilesRequest) => Buffer.from(SearchFilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => SearchFilesRequest.decode(value),
    responseSerialize: (value: SearchFilesResponse) => Buffer.from(SearchFilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => SearchFilesResponse.decode(value),
  },
  addFile: {
    path: "/pb.v1.EmbeddingService/AddFile",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: AddFileRequest) => Buffer.from(AddFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => AddFileRequest.decode(value),
    responseSerialize: (value: AddFileResponse) => Buffer.from(AddFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => AddFileResponse.decode(value),
  },
} as const;

export interface EmbeddingServiceServer extends UntypedServiceImplementation {
  embedText: handleUnaryCall<EmbedTextRequest, EmbedTextResponse>;
  searchFiles: handleUnaryCall<SearchFilesRequest, SearchFilesResponse>;
  addFile: handleUnaryCall<AddFileRequest, AddFileResponse>;
}

export interface EmbeddingServiceClient extends Client {
  embedText(
    request: EmbedTextRequest,
    callback: (error: ServiceError | null, response: EmbedTextResponse) => void,
  ): ClientUnaryCall;
  embedText(
    request: EmbedTextRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: EmbedTextResponse) => void,
  ): ClientUnaryCall;
  embedText(
    request: EmbedTextRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: EmbedTextResponse) => void,
  ): ClientUnaryCall;
  searchFiles(
    request: SearchFilesRequest,
    callback: (error: ServiceError | null, response: SearchFilesResponse) => void,
  ): ClientUnaryCall;
  searchFiles(
    request: SearchFilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SearchFilesResponse) => void,
  ): ClientUnaryCall;
  searchFiles(
    request: SearchFilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SearchFilesResponse) => void,
  ): ClientUnaryCall;
  addFile(
    request: AddFileRequest,
    callback: (error: ServiceError | null, response: AddFileResponse) => void,
  ): ClientUnaryCall;
  addFile(
    request: AddFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AddFileResponse) => void,
  ): ClientUnaryCall;
  addFile(
    request: AddFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AddFileResponse) => void,
  ): ClientUnaryCall;
}

export const EmbeddingServiceClient = makeGenericClientConstructor(
  EmbeddingServiceService,
  "pb.v1.EmbeddingService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): EmbeddingServiceClient;
  service: typeof EmbeddingServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
